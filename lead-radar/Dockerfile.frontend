FROM node:20-alpine

WORKDIR /app

COPY frontend/package.json frontend/package-lock.json ./
RUN npm ci

COPY frontend/ .

EXPOSE 3000

# Dev server so the container mirrors the local dev workflow (hot reload).
CMD ["npm", "run", "dev", "--", "-H", "0.0.0.0"]
